# Configuration and so on !


## Windows

In the powershell directory, there is my powershell profile. 

There are two functions there:
- count-ext: similar to `ls -R | grep \*.png | wc`
- count-in-file: count the occurence of a string in a given file (don't remember the use case here !)

And a script top_folders.ps1 to list the top N folder by size in the given directory. 
